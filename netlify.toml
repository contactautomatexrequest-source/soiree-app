[build]
  command = "pnpm build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "20"

# Désactiver Netlify Blobs pour éviter l'erreur d'upload
[build.processing]
  skip_processing = false

